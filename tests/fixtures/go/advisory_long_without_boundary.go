package cache

// noBoundary is documented by one long line that holds no boundary the hint list matches.
// A line can run past the limit with no boundary this checker's short word list matches at all, reporting nothing for it read exactly like approval to an agent just told to rejoin two lines. {long}
func noBoundary() {}
