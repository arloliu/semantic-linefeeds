# Configuration

**Note:**
the value is cached for one hour.

The backend is **required.**
it has no default.

- **Cache.**
  entries expire after an hour.

The retry count is _optional._
it defaults to zero.

The old flag is ~~removed.~~
use the replacement instead.
