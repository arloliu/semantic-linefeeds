# Behavior

- The value is cached, and
- the cache expires hourly.

1. The first step runs and
2. the second step follows.

- Consistency between implementations:
  - interpretation of formatting flags
  - ordering of the error chain

Tail prose after the list proves the state resets.
