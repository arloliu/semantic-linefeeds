{
  "digest": "sha256:1d14971225ca22486da6d58b58b671fb81760c6506f16a3c1cb29fa011c3a07f",
  "reason": "two protocol notes record what repairing the round's defects found, and one of them corrects an earlier note: the three false positives were recorded as one class and only two are, both inside a <pre> block, while the third sits inside a fenced block the extractor believed was closed because a run of the other mark closed it; the fence correction is noted as the one repair that adds findings as well as removing them, since prose the inversion was skipping is checked again, and as unscored on prose nobody tuned against; no floor moved, no unit record changed, and the frozen status of all 718 units is unmoved under the four repairs"
}
