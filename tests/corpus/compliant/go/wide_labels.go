package config

// The environment variable that selects the backend
// defaults to the in-memory store.

// The flag that disables the advisory threshold entirely
// takes precedence over the environment.
